spring:
  config:
    activate:
      on-profile: test
name: test-YAML
environment: testing
enabled: false
servers:
  - www.javapro-task-23-test.loc
  - www.javapro-task-23.ithillel-test.loc

---
spring:
  config:
    activate:
      on-profile: prod
name: prod-YAML
environment: production
enabled: true
servers:
  - www.javapro-task-23.loc
  - www.javapro-task-23.ithillel.loc
